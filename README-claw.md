Remaining tasks:

Install Homebrew.

Clone the openclaw repo (just for the docs).

```
corepack enable
pnpm --version
npm install -g openclaw@latest
```

Restore .openclaw from backup.

```
openclaw doctor
openclaw gateway restart
openclaw status
```
